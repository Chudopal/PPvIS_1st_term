#ifndef BASE_H
#define BASE_H
#include "cart.h"
#include <vector>

class base {
public:
	string id_base;
};
class read {
	public:
	vector<cart>read_cart(string);
	vector<base>read_base(string);
	
};
#endif // BASE_H
